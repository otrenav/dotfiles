function [xp] = sol_particular(A, b)
    %
    % Solución particular de Ax=b donde el método
    % es usando la pseudo inversa de A.
    %
    % In:
    % - A: mxn, m < n, rango(A) = m
    % - b:
    %
    % Out:
    % - xp:
    %
    % Omar Trejo Navarro
    %
    % Optimización Numérica
    % Prof. Zeferino Parada
    % Enero, 2015
    %

    % TODO: Resolver esto con Gradiente Conjugado.

    M = A*A';
    L = chol(M);    % Factor de Cholesky triangular
                    %inferior con diagonal positiva.
    z = L\b;
    w = L'\z;
    xp = A'*w;
end
