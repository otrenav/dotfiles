function  [gfx] = gradiente(fname,x)
    h = 1e-08;
    fx = feval(fname, x);
    n = length(x);
    gfx = zeros(n, 1);
    for k = 1:n
        xt = x;
        xt(k) = xt(k) + h;
        fxh = feval(fname, xt);
        gfx(k) = (fxh - fx)/h;
    end
end