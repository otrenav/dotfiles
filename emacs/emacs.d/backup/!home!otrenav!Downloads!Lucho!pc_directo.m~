%
% Resolver sistema cuadratico
% min 1/2 x'Qx + c'x
% s.a. Ax = b
%
% In:
% Q: matriz en R^nxn s.p.d.
% A: matriz en R^mxn, rango(A) = m, m < n
% b: vector en R^m
% c: vector en R^n
%
% Out:
% x: vector en R^n
% lambda: vector en R^m
%
% Prof. Zeferino Parada
% Optimización Numérica
% Omar Trejo Navarro 119711
% ITAM, 2015
%
function [x, lambda] = pc_directo(Q, A, c, b)
    n = length(c);
    m = length(b);
    K = [Q  A'; A zeros(m)];
    f = [-c;b];
    [L, U] = lu(K);
    v = L\f;
    w = U\v;
    x = w(1:n);
    lambda = w(n+1:n+m);
end

